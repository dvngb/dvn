def isAnagram(str1, str2):
    str1_list = list(str1)
    str1_list.sort()
    str2_list = list(str2)
    str2_list.sort()

    return (str1_list == str2_list)

input1 = input("First anagram").lower()
input2 = input("Second anagram").lower()

print(isAnagram(input1, input2))
